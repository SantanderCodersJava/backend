server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/bancosangue?useTimezone=true&serverTimezone=UTC
spring.datasource.username=root

spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

security.jwt.expiracao= 30
security.jwt.chave-assinatura=YmFuY29zYW5ndWU=

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# SMTP Email
api.email.from.address=${MAIL_ADDRESS}
api.email.from.name=${MAIL_NAME}

# properties Email

spring.mail.host=smtp.gmail.com
spring.mail.username=${MAIL_ADDRESS}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.mail.properties.mail.socketFactoty.port=465
spring.mail.properties.mail.socketFactoty.class = javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.socketFactoty.fallback = false

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000